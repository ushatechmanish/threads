package in.ushatech;

public class ThreadStarvation {
    // if a thread never gets its turn due to other thread going in infinite loops and  having higher priority


    // solution is to limit the time to a resource and not fiddle with the thread priorities 
}
